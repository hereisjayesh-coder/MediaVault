*.iml
.gradle/
.idea/
.kotlin/
build/
/captures
.externalNativeBuild/
.cxx/
local.properties
*.hprof
.DS_Store
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
*.apk
*.ap_
*.aab
output.json
__pycache__/
*.pyc

# Personal, machine-specific Claude Code settings (e.g. local JDK/SDK paths) — never shared.
.claude/settings.local.json

# Local manual-testing screenshots/logcat dumps — not project source.
/SS/

# Codex CLI local machine config (JAVA_HOME/ANDROID_HOME paths are machine-specific).
/.codex/

# Obsidian vault state for this repo — local note-taking tool config, not project source.
/.obsidian/

# Release-signing secrets — the actual keystore and its real path/passwords/alias must never be
# committed. Only keystore.properties.example (checked in, no real values) documents the shape;
# each machine that builds a signed release keeps its own local keystore.properties untracked.
/keystore.properties
*.jks
*.keystore
